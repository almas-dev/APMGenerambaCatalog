{% include 'header' %}

import UIKit

class {{ module_info.name }}ViewController: ParentViewController, {{module_info.name}}ViewInput {

    var output: {{ module_info.name }}ViewOutput!

    // MARK: - Life cycle
    override func viewDidLoad() {
        super.viewDidLoad()
        output.viewIsReady()
    }

    // MARK: - {{module_info.name}}ViewInput
    func setupInitialState() {

    }

}
